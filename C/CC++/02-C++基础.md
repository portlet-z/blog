# hello
```c++
#include <iostream>

using namespace std; //使用命名空间std标准的命名空间

int main()
{
	// cout标准输出
	// << 左移操作符 在C++里面功能的改造（增强） ===> C++ 语言操作符重载
	// endl \n
	cout << "Hello World." << endl;
	system("pause");
	return 0;
}
```